/**
 * frontend/js/config.js — App Configuration
 * ───────────────────────────────────────────
 * Replaces the old Firebase config.js.
 * No Firebase. Just the API base URL.
 */
const API_BASE = '/api';
